<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Administration{% endblock %}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    {% if theme == 'light' %}
        <link rel="stylesheet" href="{{ asset('css/theme-light.css') }}">
    {% else %}
        <link rel="stylesheet" href="{{ asset('css/theme-dark.css') }}">
    {% endif %}
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="container mx-auto text-center">
        <h1 class="text-4xl font-bold text-gray-800 mt-4 mb-6">Bienvenue Admin {{ admin_user.username }}</h1>
        <p class="mb-6 text-lg text-gray-600">Choisissez une action :</p>
        <div class="flex justify-center space-x-4 mb-6">
            <a href="{{ path('index_course', {'idA': admin_id}) }}" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded shadow">
                Cours
            </a>
            <a href="{{ path('qcm_index', {'idA': admin_id}) }}" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded shadow">
                QCM
            </a>
            <a href="{{ path('user_index', {'idA': admin_id}) }}" class="bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded shadow">
                Apprenants
            </a>
        </div>
        <!-- Form to change theme -->
        <form action="{{ path('app_admin_change_theme', {'idA': admin_id}) }}" method="post" class="inline-block">
            <input type="hidden" name="theme" value="{{ theme == 'light' ? 'dark' : 'light' }}">
            <button type="submit" class="bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-2 px-4 rounded shadow">
                <i class="fa fa-adjust"></i> Change Theme
            </button>
        </form>
    </div>
</body>
</html>
